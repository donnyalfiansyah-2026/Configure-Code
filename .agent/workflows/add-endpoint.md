Tambahkan endpoint API baru dengan aturan berikut:

- Definisikan kontrak request/response.
- Validasi input (misalnya Zod jika sudah dipakai).
- Tambah test untuk endpoint bila ada framework test.
- Pastikan error handling konsisten.

Output:

- Daftar file yang berubah.
- Cara mencoba endpoint (curl atau request contoh).
